# 模拟器完整复查：正常战斗路径

日期：2026-09-20。代码基线：`a8b1ec7`。项目：`C:\Users\24790\Desktop\butterfly`。

**结论：目前可以运行，但还不能把输出当作可信的罗生蝶战斗结果。问题不只剩上轮两处威力加成；技能文本转换、事件时序和人格专属机制都有正常战斗会遇到的缺口。**

前几轮检查得太局部：沿着刚修过的点继续找，没有充分检查“原始技能文本 → 结构化效果 → 实际执行 → 下一回合状态”是否一致。这次按完整结算链复查，将问题按根因合并为 20 组，包含上轮仍未修复的两项。组数不是独立 bug 数，也不代表项目只有这些问题。

范围保持不变：只看模拟器；以固定七人格、E.G.O 和第五站罗生蝶为主；E.G.O 资源默认无限；不讨论搜索算法、工程化部署、恶意输入和罕见组合。弹药、精神值及人格专属资源仍是战斗机制，不能随 E.G.O 资源一起忽略。没有修改业务代码。

## 检查与证据

- 阅读了 `battle.rs` 的选动作、拼点、攻击、防御、效果解释器、状态、被动、回合和 Boss 时序；交叉检查 `state.rs`、`setup.rs`、`effects.rs`、`scripts.rs`、`damage.rs` 与实际 JSON。
- 对照仓库缓存的技能原文、日文战斗/防御规则及 `Status_Effects.wikitext`。不把旧文档的“已完成”标签当作运行正确的证明。
- 上轮同一代码基线的 `cargo test --workspace` 为 **108 项全通过**。本轮代码未变，没有为了增加检查次数再跑同一套测试；另运行四个新的 Rust 探针程序，直接链接当前 `lcb-core`，使用真实技能做状态和伤害对照。
- 探针按需要设置状态、HP 和固定硬币，以隔离机制；这不是游戏录像逐帧验真，也不是声称这些状态都来自同一条完整战斗回放。报告区分“实测复现”“静态确认”“尚待规则核实”。
- 本轮确认 E.G.O `21207`、`21009` 的基本硬币复用次数能生效；没有将其误列为完全未实现。此前已修的攻击等级比较、上排卡轮换、攻击结束分支、破裂硬币基础威力和拉拼归属也不重复列为未修复问题。

| 环节 | 本轮检查结果 |
|---|---|
| 原始文本到效果数据 | 发现触发阶段、未破裂限定和复合效果丢失 |
| 威力与伤害 | 发现固定加成误缩放、拼点字段遗漏、实际攻击遗漏最终威力修正、硬币增伤晚一拍 |
| 行动与防御 | 发现中途混乱/死亡后继续行动、可拼点防御按普通防御执行 |
| 状态 | 发现数值分量不一致、消耗后残留、命中顺序、混乱阈值单位错误 |
| 弹药与人格被动 | 发现开场、消耗、换弹及多项核心被动未接通 |
| 回合与共鸣 | 发现绝对共鸣、罪孽名称、E.G.O 共鸣、下回合状态和效果次数问题 |
| 群攻与 E.G.O | 发现副目标漏结算，以及精神消耗、恢复人数/数值错误 |
| 罗生蝶 | 发现时态切换与回合被动不同步、击杀复用失效；技能阶段错误影响更广 |

优先级：**P1** 表示会明显改变常规战斗结果，应在信任模拟结果前修复；**P2** 表示影响具体技能/状态，同样应修，但可以排在通用结算之后。本项目不需要围绕这些问题建设大型框架。

## 1. P1：技能文本的阶段、限定条件和复合效果丢失

**实测 + 数据确认。** 位置：`tools/extract_effects.py:28`、`:1024`，以及 `data/mechanics/effects.json` 的 `956706@1`、`956704@1`、`1041404@4`、`20810.awakening@1`。

三个共同发生在文本转换层的错误：

1. **独占一行的 `[Clash Lose]` 被跳过，没有成为后续条目的阶段。** 罗生蝶 `956706` 的“过去层数减半、获得 2 Damage Down 和 4 Paralyze”中，减半和 Damage Down 被放进 `on_use`，Paralyze 又被漏掉。实测仅调用使用准备、尚未拼点，过去层数已经 **10 → 5**，并获得 Damage Down。`956709`、`956712` 同类条目也应一并修。
2. **`[On Hit without Cracking]` 直接映射为普通命中。** 实测 `956704` 只留下第二枚破裂硬币攻击，目标原有 7 威力、5 次数灼伤；结算后次数变 **4**，在硬币伤害以外多受 **7** 点灼伤。这里本来不应触发灼伤激活。
3. **一句话含多个效果时，只识别前半句仍会被算作已建模。** 良秀防御的“下回合获得 2 Fragile 和 2 Attack Power Up”只有 Fragile；鸿璐 E.G.O `20810` 的“单目标时硬币威力 +2 且伤害 +100%”变成无条件 +100%，丢了 +2 和单目标条件。`956707` 的“每 6 呼吸，拼点 +1 且增伤 10%”则只留下固定拼点 +1。

此外，Boss `[Clash Lose]` 下的“本技能攻击结束前不能使目标混乱”被抽成永久应用于本次技能的 `no_stagger_target` 标签，连正常获胜攻击也受限。

**修复/验收：** 先修解析器并重新生成数据；逐个断言上述真实技能的阶段、条件、全部子效果。不能只修改 Rust 分支，否则它收到的输入仍然错误。`unmodeled=0` 只能说明正则匹配过，不能作为技能完整实现的依据。

## 2. P1：满足条件后的固定加成，被额外加上条件中的状态数值

**实测复现。** 位置：`battle.rs:884` 的 `scaled`，调用点包括 `coin_power`、`base_power`、`damage_percent`、`clash_power`。

`scaled` 在没有 `per` 时仍按 `per=1`、`step=1` 加入 `measured`。但“有 4 层沉沦时，硬币威力 +1”的 4 层只是门槛，不是倍率。

实际技能 `1100401`：

| 目标沉沦威力 | 应得硬币加成 | 实测加成 |
|---:|---:|---:|
| 0 | 0 | 0 |
| 4 | 1 | 5 |
| 10 | 1 | 11 |

罗佳一至三技能、辛克莱二技能、奥提斯与格里高尔部分技能也走同一路径。该问题会显著虚高拼点和伤害。

**修复/验收：** 固定加成与按量缩放明确区分；没有缩放字段就只返回固定值。测试真实技能在门槛前、达到门槛、超过门槛时的结果，而不是只测试手写 `Effect`。

## 3. P1：威力计算的字段没有贯通

**上轮两项仍存在；已实测。** 位置：`battle.rs:2258`、`:2457`、`:2640`、`:2775`。

- `use_ctx.clash_power_bonus` 被技能效果写入，但 `match_power` 不读取。格里高尔 `1121403` 满足 Dazzle 条件得到 +3，保留/清除这项加成后的拼点值均为 **6**。
- 通用 `Power Up/Down`、`Attack Power Up/Down` 和 Boss 时态/招牌技能的最终威力修正只进入 `final_power`，没有进入实际攻击的累加器。`1121401` 在 Attack Power Up 为 0、1、3 时，最终威力函数返回 **11、12、14**，实际两击威力始终 **7、11**，伤害始终 **9、14**。

**修复/验收：** 让最终威力修正同时参与攻击与拼点；纯拼点加成只进入拼点。修复第 2 组后再验这一组，避免错算的加成刚好掩盖遗漏。

## 4. P1：硬币增伤在伤害之后生效，附加伤害又跳过条件

**实测复现。** 位置：`battle.rs:3110` 附近的先伤害后效果流程，`:3169`、`:3192`。

- 当前先 `compute_damage`，再执行该硬币效果，最后把 `local.damage_bonus` 累加回整次技能。结果是本硬币增伤来不及生效，却可能污染后续硬币。
- 格里高尔 `1121403` 末币“低血量或 Dazzle 时 +60% 伤害”，在目标已有 Dazzle 时，保留和删除该效果的三击伤害都是 **12、18、24**；只在攻击结束后 `damage_bonus` 多了 0.6。
- 同一技能末币的“有 Dazzle 时，追加本币伤害 80% 的忧郁伤害”被后面的直接遍历无条件执行。无 Dazzle 时，保留/删除这一条，实际总掉血为 **79 / 60**，基础三击伤害相同。该通用分支也没有按条目所写的附加伤害属性处理抗性。

**修复/验收：** 区分“本币计算前修正”“命中后状态效果”“额外伤害”；限定条件统一检查，硬币局部增伤不永久累加到技能。用同一真实末币开关条件做 A/B 测试。

## 5. P1：混乱阈值不消耗，恢复后会反复混乱；混乱中也不升级

**实测复现，有本地规则依据。** 位置：`state.rs:161`、`battle.rs:3470` 和 `end_turn` 的恢复逻辑。

`check_stagger` 每次按当前 HP 重新数所有阈值；恢复时只把 `level` 清零，从未消费跨过的阈值。同时它对“已经混乱”的单位直接返回。

良秀原有 65% / 35% 两条线：

- HP 降到 60%：混乱 level=1；继续降到 30%：仍是 **level=1**，没有进入更深混乱。
- 等待恢复，把 HP 留在第一条线以下但第二条线以上；再掉 1 HP，就又触发第一条线的混乱。

规则依据：`data/_raw/pages/ja_wiki_battle_system_details.txt:346` 至相邻混乱说明明确写出阈值不复活、混乱中继续跨线会升级、恢复后剩余阈值重新编号。

**修复/验收：** 保存尚未消费的阈值和本次混乱深度；升级不重置持续时间；恢复后删除已消费阈值。覆盖连续两击跨线、恢复后小额掉血、恢复后跨下一条线。

## 6. P1：混乱阈值以百分比存储，却直接加减 HP 数值

**实测复现。** 位置：`battle.rs:1333` 的 `tremor_burst`、`:2384` 附近招牌技能阈值效果、`:3364` 附近降低自身阈值。

`thresholds_percent` 的元素是百分比，振动爆发却直接 `+= potency`。实测当前 Boss max HP=25616，10 威力振动爆发将第一条线从 **85% → 95%**，相当于推进约 **2562 HP**，而不是 10 HP。

同类单位错误还包括 Boss `Smite the Wicked` 的最后一币推进阈值。辛克莱 `1100403` 的降低自身阈值还有更早的断点：命中效果把 `lower_stagger_percent` 写到局部 context，却没有复制回技能 context，正常路径根本不会执行；即使接通，末尾实现仍是把绝对 HP 降低量直接从百分比阈值中减去。这两层需要一起处理。

**修复/验收：** 运行期最好统一用绝对 HP 阈值，百分比只在初始化时转换。用 max HP 远大于 100 的真实单位验算 10 点推进/降低，避免小型假数据掩盖问题。

## 7. P1：行动队列没有随混乱和死亡重新验证

**实测复现。** 位置：`battle.rs:4588`、`:4686`、`:4744`、`:4805`，以及 `one_sided_attack`。

队列建立时过滤一次混乱，实际轮到行动只检查 `alive`。实测两个罪人对单只罗生蝶：Boss 先用 `956705` 群攻，使较慢辛克莱混乱；轮到辛克莱时，他仍使用 `1100401`，造成 **14** 点伤害。不是多个 Boss 或非法动作才能触发。

攻击循环也不在出血后检查攻击者存活。辛克莱剩 1 HP、带 2 威力出血，开始 `1100402` 后自己死亡，仍完成 **3 次命中**，对敌造成 **22** 点伤害。

拼点分支还需要同时重新验证双方存活/可行动，而非只检查外层 actor。

**修复/验收：** 每次动作、拼点和后续硬币前检查当时状态；区分明确允许混乱行动的 Boss。覆盖群攻先打混乱慢速队友、出血致死、前一个行动杀死后续拼点对手。

## 8. P1：可拼点防御被当成普通防御，反击每枚受击硬币都触发

**实测复现。** 位置：`battle.rs:5110`、`:4626`、`:3016`、`:3335`。

`action_defense_kind` 只根据 Guard / Evade / 其他类型分类；随后所有防御都从待拼点队列拿掉。没有可拼点防御分支。

- 罗佳 `1091304` 明确是 `[Clashable Guard]`，面对一项攻击实测 **0 次拼点**；格里高尔可拼点防御同样受影响。
- 鸿璐 `1081304` 是 `[Clashable Counter]`，实测 **0 次拼点**，对方两枚硬币命中后出现 **2 次完整反击**。其反击状态没有在首次触发后消费。
- 闪避只计算 `base_power + coin_power`，绕过通用威力修正、麻痹和防御等级补正；Guard 的 `final_power` 也没有实现 Defense Power Up/Down。

依据：`data/_raw/pages/ja_wiki_defense_skills.txt` 的可拼点防御与普通反击说明。

**修复/验收：** 分开建模普通 Guard/Evade/Counter 与可拼点防御；普通反击应按整次受击技能触发并消费。至少用以上三个固定人格防御技能各测一条完整战斗。

## 9. P1：拼点投币不触发出血

**实测复现。** 位置：`battle.rs:2207`、`:2524`、`:2581`。

出血只在实际攻击循环调用；`toss_all` 和拼点循环没有调用。给辛克莱 2 威力、3 次数出血，实际技能进行了 **2 轮拼点**，拼点后 **HP 损失 0、次数仍为 3**。

这尤其影响罗生蝶“未来”路线的出血伤害、吸血和“拼点中次数不低于 1”机制。现有 `status_count_floor` 在攻击阶段看的是本技能使用者，也不能替代“拼点主目标”的限制。

规则依据：`data/_raw/pages/ja_wiki_damage.txt:124` 明确区分出血在拼点中触发、呼吸不在拼点中触发。

**修复/验收：** 按规则的投币事件结算出血，在拼点阶段处理被限制的一方，并与第 7 组的死亡中止连接起来。不要用进入伤害阶段才扣一次的近似替代。

## 10. P1：状态写入与读取的 Potency / Count / Stack 仍不一致

**实测 + 数据确认。** 位置：`data/mechanics/status_effects.json`、`tools/extract_effects.py`，`battle.rs:1199`、`:4070`、`:5298`，`state.rs:546`、`:573`、`:590`。

这不是上次那几个状态字段已经修完后就可以关闭的问题，固定技能还会产生以下无效状态：

| 实际路径 | 写入结果 | 消费端与后果 |
|---|---|---|
| 格里高尔 S3 → 下回合 Plus Coin Boost | Potency=1，Count=0 | `prepare_use` 读 Count，实际硬币加成为 **0** |
| 奥提斯 S3 → Gloom Resist Down | Potency=1，Count=0 | 抗性读 Count，忧郁抗性实测 **1.0 → 1.0** |
| Boss 获得 Damage Down | 写 Potency | 通用减伤读 Count，不产生对应减伤 |
| 奥提斯 S3 → 下回合 Blue Sand | 数据标为 Stack，队列只保存 potency/count | 次回合得到 Potency=3、Stack=0；回合末减 Stack 后仍是 Potency=3 |

**修复/验收：** 所有状态应用通过同一套分量定义，延迟队列保留分量；对固定内容用到的状态逐个检查“技能生成值 → 引擎读取值”，尤其是强化、抗性下降、束缚和专属 Stack 状态。只测 `add_count` 手工加状态不够。

## 11. P1：状态消耗与到零清理没有覆盖所有路径

**实测复现。** 位置：`battle.rs:3035`、`:1349`、`:1363`、`:1504`，以及效果解释器的资源消耗分支。

- 呼吸暴击扣 Count 仍直接 `add_count(-1)`，没有走已增加的清理策略。实测 20 威力、1 次数呼吸，固定触发暴击后，三枚硬币仍能都暴击，最后状态保留 **22 威力、0 次数**（技能自身还会加呼吸威力）。
- `activate_status` 把 `consume_count` 再乘 `times`。罗生蝶 `956706` 末币原文“激活 2 次，失去 2 次数”，实测 **9 → 5**，多扣了一倍；且扣到零时仍绕过清理。
- `tremor_burst` 也直接扣次数，并默认任何振动爆发都扣 1；应根据技能是否另有“然后减少次数”来处理，不能统一额外扣除。
- 罗佳 `1091302`“15+ Deep Tears 时消耗 5”直接移除整个状态，实测 **15 → 0**，应剩 10。`1091303` 的“最多消耗 20”同类地删除全部存量。

**修复/验收：** 统一“扣指定数量并按规则清理”的入口；只在明确写着“全部消耗”时整项删除。覆盖正常剩余量和恰好耗尽即可，不需要极端输入。

## 12. P1：命中时先赋予沉沦/破裂，再触发被击状态，导致新状态当场消失

**实测复现，规则顺序见本地日文资料。** 位置：`battle.rs:3129` 至 `apply_sinking` 调用；依据 `data/_raw/pages/ja_wiki_damage.txt:108` 附近。

当前是基础伤害 → 技能 On Hit → 目标破裂/沉沦。资料所述顺序是基础伤害 → 受击者已有状态等 → 技能命中效果。

实际 `1100401` 对无沉沦目标施加 4 沉沦，单币结束后沉沦已经变 **0**，且比该币基础伤害多扣 **4 HP**。本来新施加的沉沦应留给后续攻击，而不是由赋予它的这次命中立即触发并清掉。

同一问题还会改变“先加次数还是先消耗”的状态续接；Butterfly 的附加伤害又在沉沦消耗/清除之后读取沉沦，末次沉沦的顺序需要一起固定。

**修复/验收：** 分开受击状态触发与攻击方 On Hit；对“目标原本没有状态”和“已有一次状态、本币续次数”分别做真实技能回归。

## 13. P1：弹药机制只完成了部分消耗，初始化、种类、换弹和转换不完整

**多项实测复现。** 位置：`setup.rs:87`、`battle.rs:1244`、`:1292`、`:1369`，`data/passives/passives.json`。

- 正常 `new_encounter` 后，理想的 The Living & The Departed 为 **0/0**，良秀 Bullet - Solitude 也为 **0**。两者原文分别要求开场各 10、开场 6；对应文本仍在未建模被动中。
- `spend_ammo` 忽略 `effect.status`，硬编码只扣 The Living & The Departed。给奥提斯 2 发 LCA Fracture Round，用真实 S3，实测 **弹药仍为 2、记录已消耗 0**。良秀弹药也不会正确扣除。
- 理想只带 2 个 The Departed，使用 S1 后却给敌人加 **2 The Living、0 The Departed**；没有保留弹药两种成分。
- 完全没有弹药仍能完成 S1 的 **2 次命中**，没有中止剩余硬币并换弹。
- 实际防御技能触发 Reload 时，**20 弹药 → 0**，只加了一个 Reload 标记，随后没有补充弹药的执行器。
- 按计划消耗量增加基础威力，没有结合实际可用弹药；在消耗机制不通的情况下仍可拿到计划消耗的加成。

**修复/验收：** 按三种弹药各自规则处理开场、消耗、补充和耗尽；保留理想每种弹药实际消耗数。用每个人格连续几回合正常出牌检验“弹药变化 → 增伤/附加状态 → 耗尽 → 补弹”，这一组必须作为整体修。

## 14. P1：共鸣数值虽然算出来，但效果端拿不到正确值

**实测 + 静态确认。** 位置：`battle.rs:738`、`:1168`、`:1399`、`:4237`、`:4953`、`:5003`。

发现四个独立接口断点：

1. **名称大小写不同。** 共鸣表存 `gloom`，效果数据用 `Gloom`，精确查找得到 0。三个忧郁技能正常结算后表为 `{"gloom":3}`；用实际 `20106.corrosion` 的 3 忧郁条件，额外攻击加权仍为 **0**。
2. **绝对共鸣赋值早了。** 在计算连续链之前就把 `a_reson_max` 复制给单位。实测全局绝对共鸣表有 **gloom=3**，单位值仍为 **0**。
3. **E.G.O 不进入共鸣。** `sin_of` 只查人格和敌人技能。使用实际 `20106` 后共鸣表为空；此探针将资源设为 9999，排除了资源限制。
4. **被动 Combat Start 早于玩家选技能和共鸣计算。** 格里高尔按忧郁共鸣获得 Lamp 读到旧回合值；`from_resonance` 赋予分支还没遵守 `per=2`。仅手工设置此前的 `Gloom` 为 0/2/4 后进入新回合，Lamp 为 **1/3/4**，暴露出既读旧值又没除 2 的问题。

此外 `table.sort()` 按 `(slot, sin)` 排序，同一 slot 下会按罪孽重新排列，不能代表部署顺序中的连续共鸣。

**修复/验收：** 统一罪孽键；按真实面板顺序形成链；全局计算完成再同步单位；选牌完成后触发 Combat Start。覆盖 3 连忧郁、忧郁被其他罪孽隔开、插入一张 E.G.O，以及下一回合换色。

## 15. P1：下回合效果提前施加，且未选择的防御也执行回合末效果

**实测 + 静态确认。** 位置：`battle.rs:1114`、`:1179`、`:1818`、`:3995`、`:4442`。

- `gain/inflict` 的 `ally` 分支在检查 `next_turn` 之前直接写状态；`compound` 外层的 next_turn 也没有传给子效果。`21009.corrosion` 的“全队下回合获得 Rhythm / 多种增伤 / Fragile”，实测立即有 Fragile，**pending 队列为空**。本回合的后续行动可能提前吃到效果，回合末又可能把它清掉。
- `apply_dashboard_phase` 不只看本回合使用的技能，还遍历可见卡、预览卡，以及未在面板上的所有人格技能。因此没有使用防御，良秀在空动作回合末仍获得 **10 Petals、2 Fragile**；罗佳防御的 Tear-sharpened/SP 结算也会自动执行。
- 格里高尔防御按 Bind/2 给慢速队友下回合 Haste，`ally_from_status` 没有被选人函数读取；默认人数为 0。Bind 又在技能回合末效果前已被过期清理。实测有 4 Bind，队友仍无 Haste、无对应待生效项。

**修复/验收：** 统一立即与延迟赋予，不让群体/复合路径绕过；技能回合末效果绑定本回合实际使用记录；按各阶段需要读取的状态完成结算后再清理。测试“选防御 / 不选防御”以及“本回合无效果、下回合有且正确过期”。

## 16. P2：条件未成立也消耗每回合/每场触发额度

**实测复现。** 位置：`battle.rs:1046`、`:1069`，条件检查在它们之后。

奥提斯 S3 的换弹条件是“剩余弹药 ≤2，最多 5 次/场”。保留足够弹药，连续检查五次这个真实效果，没有一次满足条件，`encounter:...Reload` 的使用量却已经变 **5**；以后真正缺弹也无法触发。

防御反伤也把“最多 3 次/回合”的计数用在注册反伤上，而不是每次实际受击；其每次反伤循环没有对应计数。

**修复/验收：** 在条件成立且效果实际触发后记次数；同一事件拆出的多个子效果共享一次触发记录。验收“先失败五次、后成功一次”，不能只测连续满足条件时第六次被拦截。

## 17. P1：群攻副目标只计算裸伤害，绕过完整受击结算

**实测复现。** 位置：`battle.rs:2868`。

`splash_attack` 对副目标只调用 `compute_hit_damage`、扣血和混乱判定，没有走 `apply_hit` 的状态、On Hit、防御和被动路径。

实测罗生蝶 `956706` 对三个罪人群攻：主目标获得灼伤，两个副目标**都没有灼伤**，但都受到了攻击伤害。此例为防止死亡中断提高了目标 HP，使用的是原始群攻技能和原始硬币效果。

同一代码还会漏掉副目标原有沉沦/破裂触发、闪避/Guard、相关被动和击杀统计；并按单位而非槽位计算加权。固定七人对罗生蝶的群攻就是常规场景。

**修复/验收：** 一枚币共享翻币结果，但每个实际目标执行完整受击流程；消费弹药、自伤等使用者事件只执行一次。主副目标不同状态、其中一个带防御、一个被击杀，都应得到各自正确结果。

## 18. P1：固定人格的核心被动仍未接通，部分手写替代也与原文不同

**静态确认；表内给出具体缺口，不把所有未建模文本都算成新 bug。** 位置：`data/passives/passives.json`、`tools/extract_passives.py`、`setup.rs:87`、`battle.rs:3908`、`:5375`。

| 人格 | 会影响日常使用的缺口 |
|---|---|
| 理想 10110 | 开场弹药、耗尽取消后续硬币、换弹分配、获得弹药后的 Haste；见第 13 组实测 |
| 良秀 10414 | 沉沦/振动爆发获得 Petals、达到阈值的自动追加攻击、无弹药时的追加技能和换弹；仍保留在 `unmodeled`，没有对应事件处理 |
| 鸿璐 10813 | 按本回合左槽技能转换下一手、连续成手收益、成手命中振动爆发、Kōzan 触发等仍未接通；现有 `suit_convert` 直接立即随机转换，未兑现原文 next turn |
| 罗佳 10913 | Blessing / Despair 对应正负币技能切换、Piercing Sword / Tear-sharpened 末币消费与后续精神恢复不完整；`build_use` 仍直接读固定技能数值。手写 `1091311` 还把原文 Piercing Sword 的 Damage Up/Power Up 分支换成 Tear-sharpened → Attack Power Up |
| 辛克莱 11004 | 闪避成功按忧郁共鸣概率降低混乱线的被动仍未建模 |
| 奥提斯 11114 | 高阶被动 `1111412` 只接了命中 Sheut Fracture，没有接原文 Turn Start；“耗弹额外施加”及每回合 Protection 上限也不完整 |
| 格里高尔 11214 | 忽略速度拉拼、Haste 转拼点强化、装备人数对应防御等级、Guardians of the Forest 传递等未完整接通 |

格里高尔的拉拼缺口已实测：速度 1、Boss 速度 3、敌槽瞄准队友时，`Engage` 返回不能拉拼；没有考虑其人格被动，也没有使用 Boss 的 `clash_any_speed` 标签。E.G.O 动作本身也没有可选择的 `enemy_slot`，无法表达常见的 E.G.O 抢指定敌槽拼点。

**修复/验收：** 不需要实现游戏全部人格；把这七人的关键被动列成一张逐条验收表即可。每项至少验证一次正常触发，并确认事件确实由整回合流程调用。函数存在、JSON 有条目都不等于已经接通。

## 19. P1/P2：E.G.O 的精神与目标效果还有普通使用错误

**不涉及 E.G.O 资源是否足够。** 位置：`battle.rs:629`、`:940`、`:1930`、`:3500`、`:5133`、`:5219`。

**P1：精神消耗时序。** `build_action_use` 在拼点前调用 `pay_ego`，精神值已降低，改变了本次 E.G.O 的拼点正面率。仓库 `Clash.wikitext:232` 及 `ja_wiki_damage.txt:105` 将精神消耗放在拼点结算之后、攻击前。需要将“准备使用”和“攻击前扣 SP”分开，并在相应节点判断随机侵蚀，不能简单在建对象时一并扣除。

**P2：目标人数/恢复量。** 真实效果探针：

| E.G.O | 原文要求 | 当前结果 |
|---|---|---|
| 格里高尔 Solemn Lament `21207` | 第三币命中次数 ×2 回 SP | 第三币命中 6 次，应回 12，实际回 **16**；把一、二币也算进去了 |
| 鸿璐 `20807` 觉醒 | 治疗最低 HP 比例的 2 名队友（可包含自己） | 先选 2 名其他人再强行加自己，三个受伤角色实测都被治疗 |
| 辛克莱 `21009` 觉醒 | 治疗 1+最高共鸣名其他队友，上限 4 | 最高共鸣=3、四名其他队友可治疗时，只治疗 **1 名** |

**P2：精神状态回合末效果重复执行。** `apply_sanity_states` 把分成 now/later 的两批都在回合开始执行，回合结束又执行 later。良秀 -35 SP 的 Solitude 条目“Turn End +2 Sinking Count”，实测回合开始已 **+2**，回合末累计 **+4**。

**修复/验收：** 对固定 E.G.O 逐项验证 SP 扣除节点、目标人数、第三币复用计数；不要将资源无限误解为精神消耗可以忽略。普通侵蚀与被迫侵蚀的目标选择也仍有已知建模缺口，应在宣称支持侵蚀前补齐。

## 20. P1/P2：Boss 状态切换和击杀复用没有接上正确时序

**实测 + 静态确认。** 位置：`battle.rs:4188`、`:4279`、`:4320`、`:2491`、`:2731`、`:5375`。

- **P1，时态被动晚切换。** `time_passives::turn_start` 先执行，之后生成敌人技能时才 `update_time_state`。从 Present 切到 Future 的探针，最终 active=Future，却获得 **3/3 Poise**，罪人出血次数仍为 **0**。也就是本回合技能按新时态，被动按旧时态。
- **P2，Smite the Wicked 击杀复用失效。** `reuse_on_kill` 在 `one_sided_attack` 末尾检查，但真实技能在更晚的 `[Attack End]` 才设置它。实测 `956709` 杀掉一个低 HP 罪人，另一个目标伤害 **0**、复用日志 **0**。而复用函数的下一目标选择还固定为非罪人；即使提前设置标记，敌人技能也不能正确复用到另一罪人。
- **P2，对同一目标的拼点累计方向不一致。** 记录使用 `actor_i|actor_j`，Boss 增益查询使用 `boss|sinner`；罪人先发起时记录为反方向。达到十次的增益是否出现不应由哪边先遍历决定。此项为静态确认，未另外拼出十次完整对局探针。

第 1、3、6、9、17 组还分别影响 Boss 的拼点失败惩罚、最终威力、混乱线、未来吸血和群攻，不能只修这一节就判断 Boss 已完成。

**修复/验收：** 先确定本回合时态，再执行相应回合开始被动；击杀复用作为攻击结束事件执行，目标按阵营选择；拼点累计使用同一对单位的规范化键。分别对过去/现在/未来做一次至少跨过血线和时态切换的正常回合序列。

## 核心机制尚未验证完整的部分

以下不混入“已经用探针证明”的清单，也不建议靠猜测补实现：

- **呼吸暴击率。** 当前实现和文档写 Potency%，但仓库引用正文只说“基于威力的概率”，没有证明这个系数。需补可靠规则/实战依据再确定，不能把文档的 verified 标签当证据。本轮已确认的是呼吸次数归零残留（第 11 组）。
- **拼点胜利后的攻击是否复用翻币结果。** 项目明确将复用作为当前规则，本轮没有新增游戏证据推翻它，因此没有直接列为 bug。
- **战斗默认不含的规则。** 拼点精神恢复默认 `None → 0`、共鸣等级增益、E.G.O 使用后抗性/被动等仍有缺口。罪人的罪孽抗性初始化全为 1，也没有随 E.G.O 更新的路径。这些应在“可用于实验”的说明中明确，不宜把默认输出称为游戏对局的准确复现。
- **具体被动原文与当前实装版本。** 本轮依据仓库缓存；没有用游戏客户端对七人格全部条目做版本与逐帧对照。下一步只需补关键规则的证据，不需要把个人项目扩成全游戏资料库。

## 建议的修复顺序与完成标准

按根因修，避免再沿单个例子补洞：

1. **先修数据转换与状态契约：第 1、2、10 组。** 保存正确阶段、限定条件、全部子效果和数值分量。重新生成固定内容数据，检查生成前后差异。
2. **修公共结算主线：第 3—12、15—17 组。** 完整走通“拼点 → 攻击前 → 每币 → 每目标受击 → 命中效果 → 攻击结束 → 回合结束”。不要只让孤立效果函数通过测试。
3. **把固定内容接完：第 13、14、18—20 组。** 七人格弹药与核心被动、固定 E.G.O 的关键效果、Boss 三种时态。按真实数据写案例，而不是改测试去适配目前的错误结果。
4. **最后做少量完整对局验收。** 用正常队伍、固定动作/硬币，覆盖无防御/有防御、单体/群攻、沉沦续接、耗弹换弹、共鸣与 E.G.O、混乱恢复、Boss 血线切换。选几个关键回合与游戏记录核对中间状态。

适合这个个人实验项目的“可以用了”门槛：上述常规场景能得到正确的阶段顺序、HP/SP/状态变化；固定七人格没有会颠覆技能循环的未实现被动；剩余差异是明确接受的实验假设。**当前尚未达到这个门槛，但不需要先处理部署、安全、泛化到其他人格等工程问题。**

## 复现材料位置

本轮探针位于工作目录，未放入生产代码或改动仓库测试：

```text
C:\Users\24790\Documents\Codex\2026-09-20\c-users-24790-desktop-butterfly-2\work\recheck\
  Cargo.toml
  src\bin\recheck4.rs             上轮两项威力问题
  src\bin\full_review.rs          混乱、伤害、弹药、防御、时态等
  src\bin\full_review_extra.rs    状态分量、消耗、末币增伤、中途混乱
  src\bin\full_review_ego.rs      E.G.O 恢复、群体效果、共鸣、Deep Tears
  src\bin\full_review_final.rs    换弹、精神状态、拉拼、Boss 击杀复用
```

在该工作目录上两级执行，例如：

```powershell
cargo run --manifest-path work/recheck/Cargo.toml --bin full_review --offline --quiet
cargo run --manifest-path work/recheck/Cargo.toml --bin full_review_extra --offline --quiet
cargo run --manifest-path work/recheck/Cargo.toml --bin full_review_ego --offline --quiet
cargo run --manifest-path work/recheck/Cargo.toml --bin full_review_final --offline --quiet
```

注意这些是本机审阅探针，使用了当前项目绝对路径；不是新增产品接口，也不是已经写入仓库的回归测试。报告中的对照值对应本次代码基线。
