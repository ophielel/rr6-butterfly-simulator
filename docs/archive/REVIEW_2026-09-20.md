# 模拟器审阅：先修正常战斗会遇到的问题

日期：2026-09-20；代码基线：`4aa4595`。

按个人实验项目的用途，这一版只保留影响正常战斗结果的 **5 组问题**。不讨论搜索算法、跨平台发布、非法输入和大规模运行。目标是先让固定队伍对罗生蝶的模拟跑得正确，不扩展工程建设范围。

下面的现象均在上一轮审阅中做过运行复现。现有 90 项 Rust 测试全部通过，说明需要补的是这些完整战斗流程的回归用例。这里没有用游戏录像验证全部机制，也没有修改业务代码。

## 1. 正常攒出的资源用不了 E.G.O

**位置**：`sim/crates/lcb-core/src/setup.rs:78`、`battle.rs:475`、`battle.rs:4956`；`tools/build_library.py:348`。

普通攻击产生的是 `gloom`、`sloth` 等小写资源键，E.G.O 成本却使用 `Gloom`、`Sloth`。检查能否使用和扣费时直接查成本表中的键，两边没有对上。

**实际复现**：给正常的小写资源各设为 100，仍没有任何可用 E.G.O；仅将键改成首字母大写，就出现 14 个 E.G.O 动作选项。

**怎么修**：统一资源键，在加载成本数据时转成与战斗资源相同的格式。补一个“普通攻击攒资源 → E.G.O 变为可用 → 正确扣费”的测试就能覆盖主要流程。

## 2. 第一回合漏掉被动

**位置**：`sim/crates/lcb-core/src/setup.rs:258`、`lib.rs:119–126`。

创建战斗时先执行了 `begin_turn()`，之后才给单位挂载被动。因此第一回合开始时，被动还不存在。

**实际复现**：开局 10813 的花札 suit 为 null，11114 没有 `The Udjat -Vanguard-`；隔离敌方攻击并推进到第二回合后，才分别出现花札和 2 Stack。

**怎么修**：先完成单位、被动、状态规则的初始化，再开始第一回合。直接检查 reset 后应有的被动状态，避免测试从第二回合才开始验证。

## 3. 拼点获胜后的攻击漏掉 Attack End

**位置**：`sim/crates/lcb-core/src/battle.rs:4454–4507`；对照单方面攻击分支的 `battle.rs:4520`。

单方面攻击后会调用 `apply_attack_end`，拼点获胜后的攻击却没有走这一步。这样同一个技能的攻击结束效果，会因为是否拼点而表现不同。

**实际复现**：使用实际技能 `1111403`，固定硬币并用测试等级保证拼点获胜。单方面攻击能登记“下回合获得 2 Protection”，拼点获胜后没有登记。

**影响**：也会影响李箱技能的攻击结束弹药获得、Reload 等效果。

**怎么修**：让完成攻击的分支都进入统一的攻击结束处理。先用同一个技能对照测试“单方面攻击”和“拼点获胜后攻击”，不需要为此重写整个战斗模块。

## 4. 选择上方技能时，卡牌消耗和轮换不对

**位置**：`sim/crates/lcb-core/src/battle.rs:383–393`、`battle.rs:4559–4583`。

选择上方技能时，submit 直接把下方的 `current` 改成所选技能；结算时又把原来的 `next` 下移。原本没选的下方卡被覆盖，刚用过的上方卡仍留在可用位置。

**实际复现**：seed=1、单人 10110：

```text
原牌面：current=1011002，next=1011001，preview=1011002
选择：  next 的 1011001
结算后：current 仍为 1011001，未选的底部卡已被覆盖
```

**怎么修**：提交时记录选的是哪张卡，先不要修改牌面；结算时消耗被选的卡并保留另一张。用上下两张不同的牌测试一次，确认未选卡保留、所选卡消耗和补牌都正确。

## 5. 常用状态存在“没生效”和“不消失”两类问题

**位置**：`data/mechanics/effects.json` 的 `1111403@4.attack_end`；`sim/crates/lcb-core/src/battle.rs:1053`、`3313`、`3520`、`3948`、`4210`。

**一是 Protection 写入和读取的字段不同。** 技能产生的“下回合 2 Protection”写成了 `potency=2`，减伤却只读取 Count。固定同一攻击和硬币序列，无 Protection 扣血 18，2 Potency 仍扣 18，2 Count 才降到 15。因此即使修好 Attack End，这个技能授予的减伤仍然不对。

**二是一回合状态没有过期。** 设置 2 Count Protection 和 Haste，清空动作以排除再次施加，推进回合后仍各保留 2 Count；资料文本写的是只持续一回合。

**怎么修**：先把固定队伍常用状态的数量字段和持续时间对齐，重点验证 Protection、Fragile、Haste、Bind。用真实技能施加状态，再观察伤害／速度及下回合是否消失，避免测试只手动塞入正确字段。

## 下一步建议

建议分三小步做，每步修完就跑现有测试和对应的少量回归用例：

1. **先修资源键和首回合初始化**：改动集中，直接影响整场战斗。
2. **再修拼点收尾和选牌轮换**：保证一个技能从选择到用完的流程正确。
3. **最后对齐常用状态的生效和过期**：保证连续几个回合不会累积明显偏差。

完成后，挑一段固定队伍打罗生蝶的短战斗记录，先对照前两三个回合的牌面、被动、资源和状态。对个人实验项目，这比先补复杂框架或扩大测试规模更有帮助。暂时不需要做大重构，也不用在这一步处理后续搜索算法。
