# Rule book — what the simulator implements, and where it comes from

Every rule below names its source and its verification status.  Status values
follow the project plan: `official`, `multi_source_verified`,
`single_source_verified`, `video_verified`, `unknown`, `synthetic`.
`REAL` mode refuses `unknown` and `synthetic` records.

Sources used below:

* **EN-wiki** = `limbuscompany.wiki.gg` (`Battles`, `Damage`, `Status Effects`)
* **JA-wiki** = `wikiwiki.jp/lcbwiki` → `Wiki管理/戦闘指南/戦闘システム詳細` and its
  siblings (`守備スキル`, `威力`, `破壊不能コイン`, `ダメージ計算`), cached in
  `data/_raw/pages/ja_wiki_*.txt` and re-fetchable with `tools/fetch_jawiki.py`
* **game** = in-game localisation dump (official text, keyed by internal id)

| # | Rule | Implementation | Source | Status |
|---|------|----------------|--------|--------|
| 1 | Damage = `CoinRoll x (1 + Static) x (1 + Dynamic)`, floored, min 1, min 5% of coin roll | `damage::compute_damage` | wiki.gg `Damage` | single_source_verified |
| 2 | Static = sin res + type res + off/def level + crit + `clash_count x 0.03` + observation (always 0) | `damage::compute_damage` | wiki.gg `Damage` | single_source_verified |
| 3 | Resistance → modifier: `x<0 ⇒ -0.5`, `0≤x<1 ⇒ (x-1)/2`, `x≥1 ⇒ x-1` | `damage::resistance_modifier` | wiki.gg `Damage`, `Clash` | single_source_verified |
| 4 | Off/Def level advantage = `(Off-Def)/(|Off-Def|+25)` | `damage::offense_defense_advantage` | wiki.gg `Damage` | single_source_verified |
| 5 | Clash power from levels: +1 per 3 levels, rounded down | `damage::level_clash_bonus` | wiki.gg `Clash` | single_source_verified |
| 6 | **Clash power is the sum over all coins**: both sides toss *every* remaining coin; `Final Power = Base Power + Σ(Coin Power of Heads coins)`; Match Power adds `+1 per 3 levels` for the higher skill level; resistances, defense level, attack type and sin affinity never take part | `battle::final_power`, `battle::match_power` | EN-wiki `Battles` + JA-wiki マッチの仕組み | multi_source_verified |
| 6a | Clash resolution: on a difference the lower side destroys **its first remaining coin**; on a tie **nothing** is destroyed and the clash continues; the side with coins left wins and attacks with them | `battle::resolve_clash`, `battle::destroy_first_coin` | JA-wiki マッチの仕組み | multi_source_verified |
| 6b | Attack damage accumulates: `Base Power + Coin Power` for each Heads coin, and every coin deals the power accumulated so far (4+4 ×3 → 8, 12, 16 = 36; 1+6 ×5 → 7…31 = 95) | `battle::one_sided_attack` | JA-wiki 攻撃 | multi_source_verified |
| 7 | Unbreakable coins become *Cracked* instead of destroyed and attack after the clash with coin power fixed to 1 (+1 for plus coins) | `battle::break_coin`, `battle::effective_coin_power` | wiki.gg `Clash` / Unbreakable Coins | single_source_verified |
| 7a | Paralyze: when a coin is tossed its Coin Power becomes 0 and the Paralyze count is consumed once per tossed coin | `battle::toss_all`, `battle::toss_single` | JA-wiki 麻痺 | single_source_verified |
| 8 | Stagger: thresholds are % of max HP; crossing one staggers for the current and next turn; levels give x2 / x2.5 / x3 to the type modifier | `state::StaggerState`, `battle::check_stagger` | wiki.gg `Clash` / Stagger, `Damage` | single_source_verified |
| 9 | Heads chance `H = 50 + SP`, SP clamped to [-45, 45]; units without Sanity flip at 50% | `rng::heads_chance`, `state::Sanity` | wiki.gg `Sanity`, `Clash` | single_source_verified |
| 10 | Burn: turn end, fixed damage by Potency, then Count −1 | `battle::end_turn` | wiki.gg `Status Effects`, in-game text | multi_source_verified |
| 11 | Bleed: when tossing an attack coin, fixed damage by Potency, then Count −1 | `battle::tick_bleed` | wiki.gg `Status Effects`, in-game text | multi_source_verified |
| 12 | Sinking: when hit, SP damage by Potency then Count −1; non-SP units take Gloom damage instead | `battle::apply_sinking` | wiki.gg `Status Effects`, in-game text | multi_source_verified |
| 13 | Poise: on hit, Potency% chance of a crit (1.2x, static +0.2), Count −1 on success and at turn end | `battle::apply_hit`, `battle::end_turn` | wiki.gg `Status Effects`, `Damage` | multi_source_verified |
| 12a | Rupture: when hit, fixed damage by Potency, then Count −1 | `battle::apply_hit` | wiki.gg `Status Effects` / Rupture | single_source_verified |
| 14 | Fragile: +10% damage taken per Count (max 10) | `battle::incoming_damage_modifier` | wiki.gg `Status Effects` | single_source_verified |
| 14a | Protection: −10% damage taken per Count (max 10); `<Sin> Fragility`: +10% damage taken from that affinity per Count (max 10); `<Sin>/<Type> Resist Down`: +0.1 resistance per Count | `battle::incoming_damage_modifier`, `state::Unit::resist*` | wiki.gg `Status Effects` | single_source_verified |
| 14b | Damage Up / Damage Down: ±10% damage dealt per Count (max 10) | `state::Unit::outgoing_damage_modifier` | wiki.gg `Status Effects` | single_source_verified |
| 14c | Power Up/Down (all skills) and Attack Power Up/Down (attacks only) change Final Power by the status value; Plus Coin Boost / Minus Coin Drop change Coin Power | `battle::final_power`, `battle::effective_coin_power` | wiki.gg `Status Effects`, `Damage` | single_source_verified |
| 14d | Offense/Defense Level Up/Down change the unit's levels by their Potency | `state::Unit::offense_level`, `defense_level` | wiki.gg `Status Effects` | single_source_verified |
| 14e | Bind: Speed −Potency for the turn; Haste: Speed +Count | `battle::begin_turn` | wiki.gg `Status Effects` | single_source_verified |
| 15a | Tremor: when hit by a skill that bursts Tremor, raise the Stagger Threshold by Tremor Potency, then Count −1; at turn end Count −1 | `battle::apply_effects` (`tremor_burst`), `battle::end_turn` | wiki.gg `Status Effects` / Tremor, Tremor Burst | single_source_verified |
| 16a | Charge: Count −1 at the end of each turn | `battle::end_turn` | wiki.gg `Status Effects` / Charge | single_source_verified |
| 15 | Butterfly (unique Sinking): attacker heals `The Living / 4` SP on hit; turn end converts The Living into The Departed | `battle::apply_hit`, `battle::end_turn` | wiki.gg `Status Effects`, in-game `Bufs-walpu4` | multi_source_verified |
| 16 | The Living & The Departed (unique Ammo): consumed by skills, Potency+Count capped at 20 | `battle::apply_effects` (`spend_ammo`) | in-game `BattleKeywords-walpu4` | official |
| 17 | Shield is consumed before HP and expires at the start of the next turn | `state::Unit::take_damage`, `battle::begin_turn` | wiki.gg `Clash` / Shield | single_source_verified |
| 18 | **Dashboard**: each slot shows two *selectable* skills (both usable this turn) plus one faint preview above them; using a skill consumes it and the rest rotate up, drawing a new preview | `state::DashboardSlot`, `battle::rotate_used_slots` | JA-wiki チェーンパネル ("ハッキリと見える2つのスキルとそれらの上に薄らと見える1つのスキル") | multi_source_verified |
| 18f | **Discard** deletes a selectable Skill from the Dashboard (never the preview) and the panel refills; "[Discard] N Skills of the lowest rank" and "if the other Skill in the same Skill Slot is different, [Discard] it" are implemented | `battle::discard_from_slot`, `battle::discard_lowest_rank` | wiki.gg `Status Effects` / Discard + skill text | single_source_verified |
| 18a | The composition ("Skill Amount", typically 3/2/1) is shared by all of a unit's slots; new skills are drawn **randomly** from the copies not yet placed, and the counts reset once every copy has been placed | `state::SkillDeck::{draw, reset}` | JA-wiki スキル構成 | multi_source_verified |
| 18b | A slot whose skill was never used (target died, user staggered, skill cancelled) keeps its skills | `battle::resolve_combat` | JA-wiki スキル | single_source_verified |
| 18c | Defense skills and E.G.O replace the bottom skill; using one still consumes the replaced skill | `battle::submit`, `battle::rotate_used_slots` | JA-wiki 守備スキル・E.G.Oの生成 | single_source_verified |
| 18d | Extra Skill Slots: from turn 2, the Sinner lowest in Deployment Order without an extra slot gets one, until the encounter maximum (6 in Focused Encounters) | `battle::begin_turn` | EN-wiki `Battles` / Deployment Order | single_source_verified |
| 18e | Defense skills: **Guard** gains Shield equal to its Final Power *when the unit is first attacked that turn* (and does nothing if the unit is never attacked), and replaces the unit's Defense Level with the skill's for the turn; **Evade** flips against every incoming coin, negates it when equal or higher and is lost when it fails; **Counter** strikes back at the attacker | `battle::ActiveDefense`, `battle::activate_guard`, `battle::active_defense_level` | EN-wiki `Battles` / Defense Skills + JA-wiki 守備スキル | multi_source_verified |
| 19 | Attack skills generate 1 E.G.O resource of their affinity on use | `battle::prepare_use` | wiki.gg `Clash` / Attack Skills | single_source_verified |
| 20 | E.G.O costs its listed resources and SP; Overclock uses the corrosion skill at 1.5x cost (rounded up) | `battle::pay_ego`, `battle::ego_affordable` | wiki.gg `Clash` / E.G.O Skills, Overclocking | single_source_verified |
| 21 | Unit HP `= Base + Mult x Level`; speed is rolled from the unit's range each turn | `library::IdentityStats::hp_at_level`, `battle::begin_turn` | wiki.gg `Clash` / Health, Speed | single_source_verified |
| 23 | **Imago action pattern**: six Skill Slots, a three-turn cycle, small/mid/big skill per state of time, different patterns below 66% and 33% HP, idle slots on turn 3, and the unit acts even while Staggered | `scripts::EnemyScript`, `battle::enemy_turn_skills` | wiki.gg Imago `Behavior` + JA-wiki 行動パターン | multi_source_verified |
| 24 | **States of time**: encounter starts with 10 Stacks of each; Turn Start activates the highest Stack (ties keep the current state); crossing 66%/33% HP grants 10 more of each once; switching adds 1 Temporal Disjunction | `battle::update_time_state` | wiki.gg Imago passives, in-game `Bufs_Refraction6` | multi_source_verified |
| 25 | **Stack bonus**: 0-10 → +1 Potency/Count; 11-20 → Clash Power +1, +2 Potency/+1 Count; 21-30 → Final Power +2, +3 Potency/+2 Count, applied to the state's status (Burn / Poise / Bleed) | `scripts::TimeState::stack_bonus`, `battle::time_state_bonus` | in-game `Bufs_Refraction6` (`StackPast/Present/FutureActivate`) | official |
| 26 | **Past**: hitting the Imago burns the attacker (1 Burn, +1 Count); `Kalpāgni` +2 Final Power and its last Coin deals (Stack/2) SP damage; Turn Start inflicts 5 HP Healing Down + 3 Wrath Fragility on Sinners with 10+ (Burn Potency + Count) | `battle::time_passives` | wiki.gg Imago passive `Past [過去]` | single_source_verified |
| 27 | **Present**: `Smite the Wicked` +2 Final Power and its last Coin raises the Stagger Threshold by (Stack/2); Turn Start gains (2 + #Sinners) Poise Potency and Count; no Poise Count loss on crit; critical hits deal +((Poise Potency + Count) x 2)% (max 120%) | `battle::time_passives` | wiki.gg Imago passive `Present [現在]` | single_source_verified |
| 28 | **Future**: `Bloodflower` +2 Final Power and its last Coin heals (Stack x 3) HP; Turn Start gives all Sinners +(3 + turn/2) Bleed Count; Bleed damage heals the Imago | `battle::time_passives` | wiki.gg Imago passive `Future [未來]` | single_source_verified |
| 32 | Effects with a "(N times per turn)" / "(N times per Encounter)" limit are counted; "next turn" grants are queued and applied at the next Turn Start; "At N+ [X], consume M [X] to deal +K% damage" spends the status and adds damage; "Gain Shield equal to (SP / N)% of max HP" and "for every [X] on self, gain Shield" compute Shields | `battle::apply_effects`, `state::Unit::{turn_effect_usage, pending_next_turn}` | skill effect text + wiki.gg `Status Effects` | single_source_verified |
| 33 | Defense-skill retaliation: "When hit while this unit has Shield, inflict N [X] against the attacker" fires while the Shield holds | `battle::apply_hit`, `state::RetaliateOnHit` | wiki.gg skill text | single_source_verified |
| 34 | Enemy targeting prefers Sinners with `Aggro`, then the fastest | `battle::enemy_targets` | wiki.gg `Status Effects` / Aggro | single_source_verified |
| 67 | **Defense Skills consume their Slot**: "使った守備スキルは守備スキルに変更した元のスキルごとパネルから消え、次のターンには新しいスキルがパネルに追加される" - the defense Skill and the card it replaced both leave the panel and the Slot rotates (also when the defense never fired) | `battle::{resolve_combat, rotate_used_slots}`, `state::DashboardSlot::replaced` | JA-wiki 守備スキル | single_source_verified |
| 66 | **"Next turn" statuses arrive before Speed is rolled**, so a queued Bind / Haste moves the Speed of the turn it applies to | `battle::begin_turn` | in-game `Bufs` (Binding / Agility), wiki.gg `Status Effects` | multi_source_verified |
| 81 | **Unique Ammo is spent by the pool its Skill names**: "The Living & The Departed" opens at 10 of each half and every unit spent is randomly The Living (Potency) or The Departed (Count) - 30/70 at 0+ SP, 70/30 below 0 - and the unique [Sinking] repeats the split that was consumed.  "Bullet - Solitude" and "LCA Fracture Round" are single-value pools spent from Stack.  Running out midway cancels the remaining Coins and pays for a `[Reload]`, which refills the pool (SP cost `(30 - sum)/2` for The Living & The Departed) | `battle::{spend_ammo, roll_living, reload_solitude, inflict_equal_ammo_spent}`, `passives` 1011002 / 1041402 | passive `ISeeTheDyingButterfly.`, wiki.gg `Status Effects` (Unique Ammo / Reload) | multi_source_verified |
| 82 | **"For every [X] about to be spent" is capped by the ammo the unit holds**, and Outis' Sheut Fracture rider grows by the LCA Fracture Rounds the Skill spent | `battle::base_power_per_ammo_planned`, `inflict_per_ammo` | in-game Skill text, passive `The Scepter of Horus - Replica` | single_source_verified |
| 83 | **A unit may chain to an enemy Slot without out-speeding it** when it carries Gregor's `Dazzling Lamp` or when the enemy Skill is tagged "Can Clash with this Skill regardless of Speed" ("[Unclashable]" Skills never pair) | `battle::{submit, is_unclashable}`, `state::Unit::clash_any_speed_slots` | in-game passive / Skill text, JA-wiki 戦闘システム詳細 | multi_source_verified |
| 84 | **Rodion's Blessing / Despair state follows her SP at Turn Start** ("at 0 or higher SP, gain [BlessingAlly] … at less than 0 SP, gain [DespairAlly]"), and Ryoshu's `Petals` grow by 1 whenever an enemy takes Sinking damage and by 2 on a Tremor Burst (at most 15 Stack per turn) | `battle::{begin_turn, apply_sinking, tremor_burst}`, `passives` 1091311 / 1041411 | passive `The Sword Sharpened with Tears` / `Unwithering Flower`, wiki.gg `Status Effects` (Petals) | multi_source_verified |
| 85 | **Alternate Skills referenced by a passive** (`skill1-2` / `skill3-2` on the identity page, ids 1041405 "Stories that Never Cease" / 1041406 "Magnificent End") are parsed next to the Dashboard Skills and used by the passive's Unopposed Attacks | `tools/build_library.py` (`parse_skills_from_page`), `battle::passive_follow_ups` | wiki identity page + in-game Skill ids | multi_source_verified |
| 86 | **Ryoshu's passives fire Unopposed Attacks at her Attack End**: "Bang. Bang." at 1+ [Bullet - Solitude] when the target is below -40 SP or Staggered (once per turn), "Stories that Never Cease" at 0 ammo (10 SP, then a Full Reload) and "Magnificent End" at 30+ [Petals]; one such attack cannot chain into another | `battle::{passive_follow_ups, unopposed_attack}` | passives `A Void that Cannot be Filled` / `Unwithering Flower` | multi_source_verified |
| 87 | **Hong Lu's Hand is converted at the next Turn Start**, not when the Combat Start clause resolves ("Next turn, convert the Hand on self to the one corresponding to the used Skill's Affinity") | `battle::{apply_effects, begin_turn}`, `state::Unit::pending_suit_conversion` | passive `Koi-Koi`, Skill `Shuffle Hands - Floral Deck` | single_source_verified |
| 88 | **Sin Resonance grants Offense/Defense Level** by the Skill's position on the chain: +1/+3/+3/+5/+5/+7/+7/+9/+9/+11/+11 for positions 1..11+, and an Absolute Resonance chain gives its 3rd value onwards to every Skill in it | `battle::{resonance_level_bonus, a_resonance_level_bonus, compute_resonance}`, `state::BattleState::resonance_levels` | wiki.gg `Sin Resonance` / Offense-Defense Level Gain | single_source_verified |
| 89 | **Sinclair's Jubilo Hedonista**: a successful Evade lowers his own Stagger Threshold by the Evade Skill's Power at a (5x Gloom Reson.)% chance; Ryoshu's Unwithering Flower gains [Petals] equal to the [Faint Aroma] she inflicts and deals +([Faint Aroma])% damage against Staggered targets | `battle::{apply_hit, apply_effects}`, `passives` 1100401 / 1041411 | passive text (`Júbilo Hedonista`, `Unwithering Flower`) | multi_source_verified |
| 90 | **Hong Lu's `Koi-Koi`** uses "Kōzan" (Skill `10813032`) as an Unopposed Attack at his Skill End when [HanafudaCombo] reaches 5 Potency or 0 Count; Kōzan's own `[Bright -光-]` clauses ("cancel the Skill" below 3, +10% damage per Potency, Base Power +1 at 4+, a double [Tremor Burst] at 5) are modelled, with the "sum of [X] on all targets" measurement and "Lose [X] on self" | `battle::{passive_follow_ups, apply_effects, measured_from_condition}`, `tools/extract_effects.py` | passive `Koi-Koi`, Skill `Kōzan` | multi_source_verified |
| 91 | **`… this unit had at Combat Start`** clauses read a snapshot of the status amounts taken when the turn's Combat Start phase begins ("Gain 1 additional [Deep Tears] for every 2 [Piercing Sword] this unit had at Combat Start"), exposed as `Component::CombatStart` | `battle::{resolve_combat, unit_status_value}`, `state::Unit::combat_start_statuses` | in-game Skill text | single_source_verified |
| 92 | **"Lose N SP" on use**, "Deal +30% damage for every excess Atk Weight beyond the number of actual targets" and "External effects cannot trigger this Skill to be Reused" are modelled | `battle::apply_effects`, `UseContext::attack_weight_bonus` | in-game Skill text | single_source_verified |
| 93 | **Rodion's Blessing / Despair swaps her Base Skills for the Plus / Minus Coin Skill of the same Slot** (`skill1-2` / `defense2` on the identity page, matched to their in-game ids by name: `Faded Faith` 1091305, `With the Tear-sharpened Sword` 1091306, `Arcana: Tear and Pierce` 1091307, `Weathered Pride` 1091308), so the Despair state trades Coin Power for Base Power | `tools/build_library.py` (`minus_variant`), `battle::build_use` | passive `The Sword Sharpened with Tears`, wiki identity page | multi_source_verified |
| 94 | **A "Cracked Coin" is an [Unbreakable Coin] destroyed by a Clash**: it keeps acting and its clauses still resolve (only `[On Hit without Cracking]` is skipped) and its Coin Power is fixed at 1, so "Base Power -2 for every Cracked Coin (max -6)" counts the Coins of that Skill use that the Clash cracked | `battle::{resolve_clash, apply_hit_inner, base_power_per_cracked_coin}` | JA-wiki 破壊不能コイン ("マッチで破壊されるとコイン威力が1になる"), in-game E.G.O text | multi_source_verified |
| 95 | **`[Before Attack]` resolves after the Clash**, at the first Coin toss of the attack (JA-wiki ダメージ / 攻撃の流れ puts 「攻撃前」 between the match and the Coins); the E.G.O SP cost and "for every Cracked Coin" clauses read that moment, and "Gain Atk Weight" is folded in there too | `battle::{one_sided_attack, prepare_use}`, `UseContext::before_attack_done` | JA-wiki ダメージ / 攻撃の流れ, wiki.gg `Clash` | multi_source_verified |
| 96 | **A critical hit adds a fixed 20% to the static damage modifier** (`(0.2 + Static Crit Modifiers) x (1 + Static Crit Mult Modifiers)`), and Poise's critical chance is its Potency in percentage points (capped at 99 by the status itself) | `damage::compute_damage`, `battle::apply_hit` | wiki.gg `Damage` / Critical Hits, `Status Effects` (Poise: "a Potency-based chance", max 99) | multi_source_verified |
| 68 | **Chaining to an enemy Skill Slot (focused encounters)**: a Skill whose user out-speeds an enemy Slot redirects that enemy Skill onto itself ("自分速度が相手のスロットよりも早ければ、そのスキルの使用先を自分に向けさせることができる"); a Skill already aimed at the unit may chain to it regardless of Speed, and a unit whose Speed is equal to or lower than the Slot's cannot chain ("自分の速度が相手のスロットの速度以下であり、かつ矢印が自分に向いていない場合、その攻撃にマッチすることは出来ない。互いに一方攻撃となる").  Exposed as `Action::Engage { enemy_slot }`; normal battles decide Clash partners automatically; when several Skills chain to the same enemy Slot the **last** chain wins ("同じスロットに対して複数回行った場合、敵のスキルの使用先は当然「最後に行った使用先の変更」に準拠する") and that Slot can no longer be picked up by another Sinner through its old target | `battle::{legal_actions, submit, resolve_combat}`, `state::BattleConfig::focused_encounter` | JA-wiki 戦闘システム詳細 (集中戦闘 / 幻想体戦) | multi_source_verified |
| 69 | **A hit resolves in a fixed order**: damage, then the **defender's** own effects (its buffs/debuffs oldest first, passives and "when hit" triggers), then the attacker's `[On Hit]` clauses - so a freshly inflicted [Sinking] is not consumed by the hit that applied it | `battle::apply_hit` | JA-wiki ダメージ / 攻撃の流れ ("ダメージ、被弾者のバフ/デバフ効果(古い順)、パッシブやギフトの効果、的中時効果がこの順番通りに発動する") | single_source_verified |
| 70 | **Bleed triggers on every Coin tossed, including during a Clash** ("[コイン判定時] 出血 … 出血と呼吸で扱いが違う(マッチ中に発動する/しない)"), while Poise does not; a Skill's "the main target's [Bleed] Count does not drop below 1" protects its opponent | `battle::resolve_clash`, `tick_bleed_with_floor` | JA-wiki ダメージ (スキル等効果のタイミング一覧) | single_source_verified |
| 71 | **Stagger lines are absolute HP and are consumed**: "混乱区間はステージ中復活しない" - a crossed line never comes back, crossing another line while Staggered escalates 混乱 → 混乱+ without refreshing the duration ("混乱状態のターンは更新されない"), and after recovery the remaining lines renumber.  "[Tremor Burst] raises the target's Stagger Threshold by [Tremor] Potency" moves the line by that many **HP**, not percentage points | `state::StaggerState`, `battle::{check_stagger, tremor_burst}` | JA-wiki 戦闘システム詳細 (混乱状態についての詳しい仕様) | single_source_verified |
| 72 | **`[Clashable Guard]` / `[Clashable Counter]` take part in Clashes**: such a defense Skill is submitted like an attack and arms the unit only when nothing clashes with it; a normal Counter answers the Skill that attacked it **once** and is then spent; an Evade compares its own Final Power (modifiers, Paralysis and the defense level correction included) | `battle::{resolve_combat, apply_hit}`, `DefenseKind` | wiki.gg `Battles` / Defense Skills, JA-wiki 守備スキル | multi_source_verified |
| 73 | **An Atk Weight splash resolves the full hit per target**: every extra target shares the Coin's toss result but runs its own On Hit clauses, its own Sinking / Rupture triggers and its own Guard or Evade, while the user's own events (ammo, self damage, Reuse) stay with the main target | `battle::{splash_attack, apply_hit_inner}` | wiki.gg `Battles` (Atk Weight) | multi_source_verified |
| 74 | **A `[Turn End]` upkeep clause reads the statuses of the turn it ends**: the Skills the unit actually used resolve their Turn End clauses before the statuses tick down, and an unselected defense Skill runs no upkeep at all | `battle::{end_turn, apply_dashboard_phase}` | JA-wiki スキル等効果のタイミング一覧, in-game Skill text | multi_source_verified |
| 75 | **Usage limits are spent when the clause resolves**: a "(N times per turn/Encounter)" clause whose condition failed does not consume its allowance | `battle::apply_effects` | in-game Skill text ("At 2 or fewer [LCA Fracture Round], [Reload] (5 times per Encounter)") | single_source_verified |
| 76 | **Sin Resonance**: the table is keyed by lowercase sin, counts every submitted Skill **including E.G.O**, keeps the panel order, and the Absolute Resonance value is synced to the units only after the chains are measured; a passive's `[Combat Start]` runs after the panel is submitted so it can read that Resonance | `battle::{compute_resonance, sin_of, resolve_combat}` | wiki.gg `Clash` / Sin Resonance, JA-wiki 戦闘システム詳細 | multi_source_verified |
| 77 | **An E.G.O's SP is paid at the `[Before Attack]` timing** (after the Clash is decided, before the first Coin), so it cannot change that Clash's Heads rate | `battle::{build_action_use, one_sided_attack}`, `pay_ego_sp` | JA-wiki ダメージ / 攻撃の流れ ("「攻撃前」のタイミングが発生 … E.G.Oを使用していた場合は精神力が低下する"), wiki.gg `Clash` | multi_source_verified |
| 78 | **The state of time is decided before its Turn Start passive runs**, so a turn that switches from Present to Future gains Future's bonuses, and an `[Attack End]` kill-reuse clause is resolved where it is written (the Skill repeats on the highest-HP unit of the opposing side, once per turn) | `battle::{begin_turn, apply_attack_end}` | wiki.gg `Butterfly of Entangled Lives::Imago` passives, in-game Skill text (Smite the Wicked) | multi_source_verified |
| 79 | **A flat conditional power bonus is flat**: "with 6+ [Sinking], Coin Power +1" adds 1 whatever the Sinking value; only a `per` field turns the measurement into a multiplier | `battle::scaled` | in-game Skill text | multi_source_verified |
| 80 | **A Coin's own damage clause applies to that Coin** and stays with it ("deal +60% damage" is read before that Coin's damage), and the extra-damage adders check their conditions and their own damage type | `battle::{apply_hit_inner, is_coin_damage_clause, coin_clause_applies}` | wiki.gg `Battles` (per-Coin resolution) | multi_source_verified |

| 65 | **A Clash compares both Skills' attack (Offense) Levels** (each Skill's own level modifier included); the opponent's Defense Level is irrelevant to Match Power | `battle::match_power` | JA-wiki 威力, wiki.gg `Clash` | multi_source_verified |
| 62 | **E.G.O resources are the Sinners' team pool**: only a Sinner's Attack Skill generates 1 resource of its sin, and the cost table uses the same lowercase sin keys as the state | `battle::prepare_use`, `Library::ego_cost` | wiki.gg `Clash` (Attack Skills / E.G.O Resources), JA-wiki 戦闘システム詳細 | multi_source_verified |
| 61 | **Which component a bare "Gain/Inflict N [X]" fills** depends on the status: Potency for the damaging statuses (Burn / Rupture / Sinking / Tremor / Poise), Count for Protection / Fragile / Haste / Bind / Charge, whose own text measures the effect by Count | `data/statuses/statuses.json` (`primary`), `tools/extract_effects.py` | JA-wiki 戦闘システム詳細 (「火傷を１付与」= +1 Burn Potency), in-game `Bufs` / `BattleKeywords` | multi_source_verified |
| 64a | **Status values reaching 0 removes the status**: "In single-value or double-value modes, if one or more of the values reach 0, the status effect is removed from the unit" - applied whenever a value is **consumed** (Burn / Bleed / Sinking / Rupture / Tremor / Poise / Charge ticks, `lose_status_count`, `consume_surplus_status`, `halve_status`), with per-status policies: `either_zero` (double-value), `count_zero` (Protection / Fragile / Haste / Bind / Charge), `both_zero` (Butterfly: "expires when both The Living and The Departed reach 0") and `none` (Unique Ammo, Stack statuses) | `battle::tick_status`, `data/statuses/statuses.json` (`expiry`), `data/mechanics/status_effects.json` | wiki.gg `Status Effects` (Overview), JA-wiki 戦闘システム詳細 | multi_source_verified |
| 64 | **Unbreakable Coin clauses are per Coin and conditional**: "At less than N% HP, convert the final Coin / all Coins into [Unbreakable Coin]s" only converts while the unit is below that HP, and each clause marks the Coins it names (a guard/E.G.O Coin printed as `[Unbreakable Coin]` is unconditionally unbreakable) | `battle::prepare_use`, `tools/extract_effects.py` | wiki.gg `Clash` / Unbreakable Coin, in-game Skill text | multi_source_verified |
| 63 | **Unbreakable Coins decide the Clash aftermath**: a Skill that loses a Clash keeps its Unbreakable Coins, whose **own Coin Power becomes 1** (later Coin Power increases still apply) and which **keep the result they were tossed with**; they attack through the normal Base Power / per-Coin accumulation flow, so the order of a won Clash is the winner's attack and then the loser's Unbreakable Coins.  "[Attack End] activates only once after an Attack Skill has used all of its Coins" - Coins destroyed in the Clash do not count as used while Unbreakable Coins do, so a losing Skill reaches its Attack End only when every Coin it had was an Unbreakable Coin | `battle::clash_loser_follow_up`, `one_sided_attack` | JA-wiki 破壊不能コイン, wiki.gg `Clash` (Attack End / Unbreakable Coin) | multi_source_verified |
| 60 | **One-turn statuses expire**: a status whose text says "for one turn" / "for this turn" is removed at Turn End (statuses granted *by* a Turn End clause survive into the next turn); `[Attack End]` also resolves after a Clash-winning attack ("Activates only once after an Attack Skill has used all of its Coins") | `battle::apply_status_phase`, `rotate_used_slots` | wiki.gg `Status Effects` / `Clash`, in-game `Bufs` | multi_source_verified |
| 59 | **Butterfly** (unique Sinking, `SinkingWhite`): The Living = Potency, The Departed = Count; "When hit, the attacker heals (The Living / 4) SP (min 1, rounded down)"; "When hit, and if this unit's SP is at less than 0, take ([Sinking] Potency / 5) Gloom damage for every value of The Departed (max 30, rounded down, half to Non-SP Units)"; "Turn End: reset The Departed to 0; then gain [Sinking] equal to The Living and convert The Living into The Departed"; "no external effects can raise the amount of Butterfly inflicted" | `battle::{apply_hit, end_turn}` | in-game `BattleKeywords` / `Bufs` (SinkingWhite), wiki.gg `Status Effects` (the raw wikitext writes `({{StatusEffect|Sinking}} Potency/5)`, i.e. the **[Sinking] status**, while The Living is written plainly) | official |
| 58 | **E.G.O Corrosion is not a player choice**: the player submits Awakening or Overclock; Corrosion fires **randomly** after paying when the remaining SP is -25..-34 (25%), -35..-44 (75%) or -45 (100%), it keeps the main target but re-draws sub-targets indiscriminately, and during an Overclock the surcharge is refunded; the E.G.O Corrosion state at -45 SP forces it | `battle::{corrosion_chance, build_action_use}`, `splash_attack` | JA-wiki 戦闘システム詳細 (ランダム侵蝕の仕様 / オーバークロック), wiki.gg `Sanity` | multi_source_verified |
| 57 | **Support Passives are not applied**: the extracted ones are keyed by identity but the team's support slots are a deck-building choice this project has no data for | `Simulator::attach_passives` | project decision, see `docs/STATUS.md` | n/a |
| 56 | **Reuse**: "Then, Reuse this Coin (N times per Skill)" gives that Coin up to N extra uses per Skill use; the Coin is **tossed again** each time (JA-wiki 守備スキル: "再使用はコインを投げるごとに「スキルを使用した」という判定になり"), its On Hit clauses resolve again, "[Reuse - ...]" clauses only resolve on a Reused Coin (wiki.gg `Clash` trigger table), and if the target is gone the Reuse picks a new one automatically (JA-wiki ダメージ *3) | `battle::one_sided_attack`, `Effect::{reuse_coin, reuse_only, reuse_from_status}` | wiki.gg `Clash`, JA-wiki 戦闘システム詳細 / 守備スキル / ダメージ | multi_source_verified |
| 55 | **Forced E.G.O Corrosion**: a Sinner at -45 SP who owns a Corrosion Skill corrodes instead of Panicking; their submitted action is replaced by that Corrosion Skill aimed at a random enemy, and their SP resets to 0 after the turn | `battle::apply_sanity_states`, `resolve_combat` | wiki.gg `Sanity`, `Clash` | multi_source_verified |
| 54 | **Status riders**: `[When Clash ends] ...` clauses run for both units after a Clash resolves and `[When hit] ... on the attacker` clauses run for the unit that was hit (their "(once per turn)" limits are honoured) | `battle::apply_status_event` | wiki.gg `Status Effects` (Blessing / Despair) | single_source_verified |
| 53 | **Status behaviour comes from the status's own text**: `data/mechanics/status_effects.json` holds each status's `[Turn Start]` / `[Turn End]` / continuous clauses, parsed by `tools/extract_status_effects.py` from the in-game text (and the wiki text for statuses the dump does not carry, e.g. Rodion's Blessing / Despair / Protecting Sword / Tear-sharpened).  The engine runs the phase clauses while a unit holds the status (`remove_at_turn_end` for "Expires at Turn End") and adds the continuous ones to the damage formula.  Example: `Base Attack Skills deal +(Stack x 10)% damage (max 30%)` (Tear-sharpened), `Turn Start: gain 1 [Defense Level Up] for every Stack (max 5)` (Protecting Sword), `For every 1 ([Sinking] + [Burn]), take +0.5% damage from Base Attack Skills (max 10%)` (Dazzle) | `effects::StatusBook`, `battle::apply_status_phase`, `status_modifiers` | in-game status text, wiki.gg `Status Effects` | multi_source_verified |
| 52 | **Sanity, Low Morale and Panic**: SP lives in [-45, 45]; a Sinner at -45 stays there until the next Turn Start, then either Corrodes or Panics; `-30` or lower is Low Morale, `-45` is Panic; the Panic Type's Low Morale / Panic clauses apply once on the earliest Turn Start (and at Turn End when written so); a Panicking Sinner does not act; the SP resets to 0 after a Panic turn.  The wiki calls Low Morale chance-based but gives no chance, so the simulator applies it deterministically and records that | `battle::apply_sanity_states`, `apply_panic_turn_end`, `data/mechanics/panic_types.json` | wiki.gg `Sanity`, `Clash` | multi_source_verified |
| 51 | **Identity / enemy Passives** (`data/passives/passives.json`): modelled clauses run at `[Combat Start]` / `[Turn Start]` / `[Turn End]` / `[Attack End]`, continuous clauses (`Deal +N% damage for every [X] on self`, `Take -(SP/2)% HP damage`, `Deal +(-SP/2)% damage with Base Skills`) modify the unit's damage, `On Tails Hit` fires on Tails and `On Hit with a Base Attack Skill` rides on attack hits.  Clauses not modelled are listed by `Simulator::passive_gaps()` and in `docs/COVERAGE.md` | `effects::PassiveBook`, `battle::apply_passive_phase`, `passive_modifiers` | in-game `Passives*.json`, wiki.gg identity/enemy pages | single_source_verified |
| 50 | **Skill phase order**: `[Combat Start]` (for the Skill chosen this turn) → `[On Use]` → `[Before Attack]` → Coin tosses → `[On Hit]`/`[Heads Hit]` → `[Attack End]`; `[Turn Start]`/`[Turn End]` clauses of every Skill in a unit's kit (including the defense Skill) resolve once each | `battle::prepare_use`, `apply_dashboard_phase` | wiki.gg skill effect text, `Battles` | multi_source_verified |
| 49 | **Enemy targeting**: `Targets Randomly`, `Targets the unit with the most HP` and `Prioritizes targets that have the most [X]` pick the enemy's target; `Aggro` still raises the priority of a Slot, then Speed | `battle::enemy_targets` | wiki.gg `Status Effects` / Aggro, skill effect text | single_source_verified |
| 48 | **Sub-target damage**: "Deal -N% damage against sub-targets" scales the Attack Weight splash hits | `battle::splash_attack` | wiki.gg E.G.O `Harmony` | single_source_verified |
| 47 | **Healing reaches named allies**: `Heal N SP`, `Heal N SP to self and M other allies with the least SP`, `Heal M allies with the lowest HP percentages by (...)`, `Heal SP equal to [X] Potency on target (max N)`; selections are `self` / `all_allies` / `lowest_hp` / `lowest_sp` / `slowest` / `random` (random picks from the replayable RNG) | `battle::ally_targets`, `heal_percent_hp`, `heal_from_status` | wiki.gg `Status Effects` (Healing), E.G.O pages | multi_source_verified |
| 46 | **Tremor amplitudes**: "trigger [Amplitude Conversion] into [Tremor - Decay]" stores a marker on the unit (`Amplitude Conversion: <name>`); "If target is in an [Amplitude Conversion] or [Amplitude Entanglement] state" reads it | `battle::amplitude_conversion`, `has_amplitude`, `amplitude_of` | in-game `BattleKeywords` (Tremor), wiki.gg `Status Effects` / Tremor | single_source_verified |
| 45 | **Coin triggers and self-costs**: `[Heads Hit]` resolves on top of `[On Hit]` only on Heads; `[On Kill]`/`[On Target Kill]` once per Skill; `[On Evade]` on the defense Skill; `[Before Attack]` after On Use; self-damage obeys "does not reduce HP below 1" / "does not Stagger"; "(N times per Coin/Skill)" limits | `SkillMechanics::{heads_hit,on_kill,on_evade,before_attack}`, `self_damage` | wiki.gg `Clash`, skill effect text | multi_source_verified |
| 44b | **Suit (Hanafuda)**: "convert the Suit in this unit's Hand to a random Suit that corresponds to one of this unit's Base Attack Skills"; the Suit grants its Skill its Base Power bonus (Pine Crane +2 to Skill 1, Susuki Grass +1 to Skill 2, Blue Sakura +1 to Skill 3) | `battle::suit_convert`, `state::Unit::suit` | in-game `BattleKeywords` (HanafudaOne/Two/Three) | single_source_verified |
| 44a | **Butterfly split**: `[Butterfly](The Living)` is Potency and `[Butterfly](The Departed)` is Count; "(Chance to flip Heads)% chance to inflict The Departed" per Stack | `Effect::butterfly_part`, tag `butterfly_split` | wiki.gg `Status Effects` / Butterfly | single_source_verified |
| 44 | **Attack Weight**: a Skill with N Attack Weight hits N Slots; the main target goes through the Clash/one-sided path and the remaining Slots take the same Coin Rolls without Clashing (splash hits skip per-target ammo bookkeeping) | `battle::splash_attack` | wiki.gg `Clash` / Attack Weight | single_source_verified |
| 43 | Status grants respect the status's structure: Stack-based statuses (In the Past/Present/Future, Temporal Disjunction, Dazzle, Faint Aroma, Lamp, Aggro, Blue Sand, …) are written to Stack, Potency/Count statuses to Potency/Count; the structure is derived per status in `tools/build_library.py` (game text + wiki text + explicit overrides) | `data/statuses/statuses.json`, `battle::apply_effects` | in-game `Bufs*` text, wiki.gg `Status Effects` | multi_source_verified |
| 42 | Imago clauses: "Target cannot be Staggered until this Skill's Attack End", "[Hit after Clash Lose]" gates (the skill must have lost the Clash), "At less than N% HP, ...", "Deal +([In the Past/Present/Future] x N)% damage", "Deal +([X] Count on self)% damage", "If this unit has 20+ [X] Potency, consume up to 20 surplus ... to deal +(consumed x N)% damage", "If target is killed, Reuse this Skill on the target that has the highest HP (once per turn)", "treat the target's Pierce Resist. as Weak (1.5)" | `battle::{apply_hit, one_sided_attack, apply_effects}` | wiki.gg Imago skill text | single_source_verified |
| 40 | **Stations 2-4 (illusory butterflies)**: 1 HP, 333 Shield on the first Turn Start; Eclosion is Unclashable/Target Fixed, deals 0 damage and ends the Encounter — unless the Shield broke, in which case the stage runs one more turn (the choice event) before ending; **Segmentation** removes 1 Stack from the Imago in the campaign per hit as a main target (once per Coin), heals the attacker 10 SP (once per turn per Sinner) and grants the Imago +5 Stacks at Combat End if the unit was never hit | `state::CampaignState`, `battle::apply_hit`, `battle::end_turn`, `scripts::Segmentation` | wiki.gg Illusory Butterfly pages + JA-wiki part info | single_source_verified |
| 41 | **Campaign**: station 5 starts from the Pupa's remaining HP (excluding Shield) and 10 Stacks of each state; the earlier choice events disable components of the Past/Present/Future passives (`Simulator::section5`, `CampaignState::disabled_passives`) | `Simulator::section5`, `battle::time_passives` | JA-wiki station-5 notes, wiki.gg choice events | single_source_verified |
| 39 | Boss scaling clauses: "Final Power/Clash Power +N for every M [X] on the main target / on self (max K)", "Deal +(In the Past/Present/Future x N)% damage", "Halve [In the Past]", "while clashing the main target's Bleed Count does not drop below 1" | `battle::apply_effects`, `battle::tick_bleed_with_floor` | wiki.gg Imago skill text | single_source_verified |
| 36 | Ammo- and spent-resource scaling: "Base Power +N for every [X] about to be spent by this Skill", "Deal +([X] spent x N)% damage", "At N+ [X], consume up to M [X]" (the consumed amount feeds the damage bonus) | `battle::prepare_use`, `battle::apply_effects` | skill effect text | single_source_verified |
| 37 | Critical modifiers: "If target's SP is below 0, boost crit chance proportional to target's SP", "If user has N+ [X] Count, +M% Critical Damage" | `battle::apply_hit`, `battle::apply_effects` | skill effect text + wiki.gg `Damage` / Critical Hits | single_source_verified |
| 38 | Self-inflicted Stagger Threshold reduction ("Lower user's Stagger Threshold by N% of damage dealt") and `<X> Resist Down`/`<Sin> Fragility` counting | `battle::apply_hit` | skill effect text, wiki.gg `Status Effects` | single_source_verified |
| 35 | **Sin Resonance**: 2+ Skills of the same affinity on the Dashboard; **Absolute Resonance** = 3+ *consecutive* same-affinity Skills; separate A-Reson chains count separately; A-Reson also counts as Reson; skill effects read the highest resonance ("Gain (highest Reson.)", "4+ A-Reson") | `battle::compute_resonance`, `battle::highest_resonance`, `battle::gain_from_resonance` | wiki.gg `Resonance` + JA-wiki 罪悪共鳴 | multi_source_verified |
| 30 | **Station 1 (the Pupa)**: six Skill Slots, turns 1-3 = 3x Fluttering Havoc + 3x Pulverization (turn 3 adds The Quickening); encounter start grants 1.3% of max HP as Shield and its HP never falls below 90%; if the Shield is fully consumed before the end of turn 3 it plays pattern a next turn (Entangled Life x3 + The Quickening) | `data/mechanics/enemy_scripts.json`, `state::Unit::take_damage` (`hp_floor_percent`), `battle::enemy_turn_skills` | wiki.gg Pupa page + JA-wiki 行動パターン | multi_source_verified |
| 31 | "End the Encounter" skills (The Quickening, Eclosion) finish the station: `Winner::EncounterEnded`, not a wipe; "This Attack Skill deals 0 damage" and "Does not take damage for this turn" are honoured | `battle::ends_encounter`, `battle::apply_effects` (`zero_damage`, `no_damage_taken`) | wiki.gg Pupa / Illusory Butterfly pages | single_source_verified |
| 29 | Temporal Disjunction: take +(Stack x 15)% damage (max 150%); Turn Start at 10 Stack grants 5 Fragile | `battle::temporal_disjunction_bonus`, `battle::begin_turn` | in-game `Bufs_Refraction6` (`TimeGap`) | official |
| 22 | Boss numbers (HP, resistances, coin power, effect text) for the fixed content | `data/enemies/*.json`, `data/identities/*.json` | wiki.gg pages + in-game text keyed by official id | multi_source_verified |

## Unknown rules (explicitly *not* guessed)

| Rule | Handling |
|------|----------|
| SP gained/lost per clash | The wiki documents that the base factors changed on 2023-06-01 but not the current values → `BattleConfig::sp_on_clash_win/lose = None`, which is treated as 0 **and logged as an unresolved rule**. Set the fields to change it. |
| Client RNG algorithm | Not observable from the permitted sources → the simulator uses a documented xoshiro256** (`rng.rs`); only the *probability model* comes from the wiki. Marked `synthetic`. |
| Ammo split (The Living vs The Departed) | In-game text only says "randomly determined" → 50/50, recorded as an assumption in `data/mechanics/effects.json` (`"assumption": "random"`). |
| Which coin a clash destroys | The JA-wiki says "1コイン目から順に一つ破壊" (from the first coin in order) → implemented as *the first surviving coin*. |
| Coin flips during the winner's attack | The final clash round already tossed every coin; the attack reuses those results (the accumulated power of the last coin equals the Final Power the clash used). |

## Not implemented (and why)

| Feature | Status | Note |
|---------|--------|------|
| Illusory Butterfly damage transfer ("Origination") | `NOT_IMPLEMENTED` | Out of scope: the deliverable is the Section 5 Imago; the butterfly data and its Segmentation passive exist for completeness but the campaign is optional (`Simulator::section5`). |
| Section 5 choice event (Sunset Wayfarer) and the earlier stations' choices that disable Past/Present/Future components | `NOT_IMPLEMENTED` | The encounter starts in `BattleConfig::initial_time_state` instead (the wiki ties the real starting state to those choices). |
| HP Healing Down / Wrath Fragility / Gloom Fragility etc. | `NOT_IMPLEMENTED` | The Past passive and several skills apply them and they show up in the state, but their own effects (healing reduction, damage amplification by affinity) are not yet part of the damage formula. |
| Illusory Butterfly Eclosion / encounter-end skills | `NOT_IMPLEMENTED` | "End the Encounter" skills are extracted but not acted upon. |
| Stations 2-4 (the illusory butterflies): 1 HP + 333 Shield, unclashable Eclosion, "hit as main target knocks a Stack off the Imago" and the choice event that disables a passive component | data only | `data/enemies/9564`-`9566` and `9572`-`9574` are loaded with their passives; the encounter itself is not scripted yet (the Section 5 starting state is set through `BattleConfig::initial_time_state` instead). |
| Sin Resonance **level bonus** (the +1…+11 Offense/Defense Level table) | `NOT_IMPLEMENTED` | The resonance *counts* are implemented; the level-gain table on wiki.gg is malformed in the fetched markup (rows have fewer cells than columns), so the exact gain per chain length is not applied. |
| Panic / Low Morale, E.G.O Corrosion forced actions | `NOT_IMPLEMENTED` | Sanity is modelled, the panic tables are not loaded. |
| Focused-encounter Parts (Core/Part splitting, part destruction) | `NOT_IMPLEMENTED` | Only the core unit is instantiated. |
| Multiple enemy skill slots | `NOT_IMPLEMENTED` | One slot per enemy; a clash only happens with the unit the enemy targeted. |
| E.G.O passives, E.G.O gifts, Observation Level | `NOT_IMPLEMENTED` | Observation Level is 0 in the game itself (wiki.gg `Damage`). |

`Simulator::unknown_rules()` and `Simulator::strict_blockers()` return these
lists at runtime; `python/demo.py --strict` prints them.
